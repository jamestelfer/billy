dist/

# ASK CLI project state: contains the skill id, and is regenerated by
# `just skill-deploy`.
.ask/
result
.claude/settings.local.json

# Captured Alexa request bodies contain live credentials (apiAccessToken,
# userId, deviceId). Never commit them.
capture/
*.body
.envrc.private
